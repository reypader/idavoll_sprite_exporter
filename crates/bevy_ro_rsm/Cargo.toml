[package]
name = "bevy_ro_rsm"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.18", default-features = false, features = [
    "bevy_render",
    "bevy_asset",
    "bevy_log",
] }
ro-rsm = { path = "../ro-rsm" }
anyhow = "1"
